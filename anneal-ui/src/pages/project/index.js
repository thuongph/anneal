import React from 'react';
import { Typography } from 'antd';

const { Paragraph } = Typography;

const Project = () => {
    const text = "this is project page"
    return (
        <Paragraph>{text}</Paragraph>
    )
}

export default Project;
