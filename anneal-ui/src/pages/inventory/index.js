import React from 'react';
import { Typography } from 'antd';

const { Paragraph } = Typography;

const Inventory = () => {
    const text = "this is inventory page"
    return (
        <Paragraph>{text}</Paragraph>
    )
}

export default Inventory;
