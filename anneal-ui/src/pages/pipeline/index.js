import React from 'react';
import { Typography } from 'antd';

const { Paragraph } = Typography;

const Pipeline = () => {
    const text = "this is pipeline page"
    return (
        <Paragraph>{text}</Paragraph>
    )
}

export default Pipeline;
